# FastAPI base URL for the typed API client (lib/api.ts).
# Defaults to http://localhost:8000 if unset.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# When "true", the API client returns mock data from lib/mock.ts instead of
# hitting the backend. Useful for demos and frontend-only development.
NEXT_PUBLIC_MOCK_API=true
